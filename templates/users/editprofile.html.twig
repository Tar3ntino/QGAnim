{% extends 'base.html.twig' %}

{% block title %}
	Modification du profil de
	{{app.user.firstname}}
	{{app.user.name}}
{% endblock %}

{% block body %}
	<div class="container">
		<div class="container-fluid">
			<div class="row">
				<div class="col-4"></div>
				<div class="col-4">
					{{form(form)}}
					</div>
					<div class="col-4"></div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
